node_modules/
coverage/
*.log
.env
.DS_Store
.terraform/
*.tfstate*
tfplan
tfplan.json
.terraeye-state.json

# Keep the GitHub Action bundle committed so `uses: org/terraeye@v1` works.
# Ignore other build artifacts.
dist/cli.js
dist/*.map
